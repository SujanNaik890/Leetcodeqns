class Solution {
    public int countCommas(int n) {
        return Math.max(n-999,0);
        // int res=0;
        // int coma=n-999;
        // if(coma > 0)res+=coma;
        // return res;


        // if(n<1000)return 0;
        // return n-999;
       }
}